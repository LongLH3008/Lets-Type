export enum Difficult {
    'Easy' = 'easy',
    'Medium' = 'medium',
    'Hard' = 'hard',
    'SuperHard' = 'super_hard'
}

export enum TypingMode {
    'quote' = 'quote',
    'word' = 'word',
    'timer' = 'timer',
}

export enum Active {
    'active' = 0,
    'non-active' = 1,
}

export enum Available {
    'public' = 0,
    'private' = 1
}